export { HomeScreen as default };

import { Text }from 'react-native';
import MainView from '@/components/MainView';

const HomeScreen = () => (
    <MainView>
        <Text>Home</Text>
    </MainView>
);
